'use client';

import AdminDashboard from '@/components/galaxy/profile/AdminDashboard'; // Reuse the existing component!

export default function ReportsFeed() {
    return (
        <div className="max-w-3xl mx-auto">
            <AdminDashboard />
        </div>
    );
}
